##Project 1 submission##
This repository contains the analysis and predictive modeling for Project 1.

## How to Run the Script ##

### 1. Prerequisites
To run the R script (`project1v3.R'), you will need RStudio downloaded on your computer along with the following packages:
# `tidyverse`
# `rpart`
# `rpart.plot`

To instal these packages you should copy the line of code below into the R console
```R
install.packages(c("tidyverse", "rpart", "rpart.plot"))
